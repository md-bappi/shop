import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/" className=" uppercase text-lg font-semibold text-bg-btn">
      shop
    </Link>
  );
};

export default Logo;
